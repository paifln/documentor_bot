Промпты фактически хранятся в `/prompts` (корень проекта), не здесь —
см. `Settings.prompts_dir` в `app/config/settings.py`. Эта директория
оставлена как расширяемая точка для промптов, специфичных для будущих
провайдеров (Anthropic/Google), если понадобится их разделить.
